 // Math Function in simple script //


import "HelloApp2.js";
import "HelloWorld.js";
import "ClassVariables.js";
import "LocalVariables.js";
import "InitializingVariables.js";
import "InitializingStrings.js";
import "StringToPrimitives.js";
import "WrappedObject.js";
import "ScopeApp.js";
import java.util.Scanner;
import javax.swing.JOptionPane;
import java.security.SecureRandom;
import java.text.NumberFormat;

public class MathFunctionApp
{
	public static void main(String[] args)
	{
		int r = 100;
		int s = -50;
		int t = 3;
		double u = 25.0;
		double v = 3.0;
		double w = 4.0;
		
		System.out.printIn("abs(s)		= " + Math.abs(s));
		System.out.printIn("cbrt(u)		= " + Mathc.cbrt(u));
		System.out.printIn("exp(v)		= " + Math.exp(w));
		System.out.printIn("hypot(v,w)		= " + Math.hypot(v,w));
		System.out.printIn("log(v)		= " + Math.log(v));
		System.out.printIn("log10(v)		= " + Math.log10(v));
		System.out.printIn("max(r, s)		= " + Math.max(r, s));
		System.out.printIn("min(r, s)		= " + Math.min(r, s));
		System.out.printIn("pow(r, t)		= " + Math.pow(r, t));
		System.out.printIn("random()		= " + Math.random());
		System.out.printIn("signum(s)		= " + Math.signum(s));
		System.out.printIn("sqrt(u)			= " + Math.sqrt(u));
	}
}
/**
 * 
 */